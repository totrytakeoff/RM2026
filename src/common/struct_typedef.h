#ifndef STRUCT_TYPEDEF_H
#define STRUCT_TYPEDEF_H

#include <stdint.h>

/* Project-specific aliases (standard exact-width types come from <stdint.h>) */
typedef uint8_t bool_t; /* 0/1 boolean stored in one byte */
typedef float   fp32;
typedef double  fp64;

#endif




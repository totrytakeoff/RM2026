#ifndef ROBOT_FIRMWARE_H
#define ROBOT_FIRMWARE_H

#include <stdbool.h>

/** Initialize the robot application and create its RTOS tasks. */
bool RobotFirmware_Init(void);

#endif /* ROBOT_FIRMWARE_H */
